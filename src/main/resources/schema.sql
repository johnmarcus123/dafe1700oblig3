CREATE TABLE Bestille (
                          id INT AUTO_INCREMENT PRIMARY KEY,
                          film VARCHAR(255),
                          antall INT,
                          fornavn VARCHAR(255),
                          etternavn VARCHAR(255),
                          telefonnr VARCHAR(255),
                          epost VARCHAR(255)
);
